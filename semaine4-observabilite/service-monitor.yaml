apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sre-app-monitor
  namespace: default
  labels: 
    release: monitoring
spec:
  selector:
    matchLabels: 
      app: sre-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s