apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sre-app-ingress
spec:
  rules:
    - host: sre-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sre-app
                port:
                  number: 80